---
title: "Get forecast prices"
openapi: "GET /me/addresses/{addressId}/forecast-prices"
---

`GET /consumer/v1/me/addresses/{addressId}/forecast-prices`

Set the key and address ID as described in [Quick start](/consumer-api/quickstart). The curl sample uses environment variables; no interactive requests are sent from these docs.

See [Forecasts](/consumer-api/forecasts) for cutoff and empty-result behavior.
