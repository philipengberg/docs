---
title: "List addresses"
openapi: "GET /me/addresses"
---

`GET /consumer/v1/me/addresses`

Set the key as described in [Quick start](/consumer-api/quickstart). The curl sample uses environment variables; no interactive requests are sent from these docs.

See [Time and ranges](/consumer-api/time-and-ranges) and [Data interpretation](/consumer-api/data-interpretation) before combining results.
